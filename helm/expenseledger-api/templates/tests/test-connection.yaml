apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expenseledger-api.fullname" . }}-test-connection"
  labels:
    {{- include "expenseledger-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expenseledger-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
