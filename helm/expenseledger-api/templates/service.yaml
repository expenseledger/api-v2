apiVersion: v1
kind: Service
metadata:
  name: {{ include "expenseledger-api.fullname" . }}
  labels:
    {{- include "expenseledger-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expenseledger-api.selectorLabels" . | nindent 4 }}
